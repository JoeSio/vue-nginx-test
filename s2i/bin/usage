#!/bin/bash -e
cat <<EOF
This is the vue-nginx-test S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> vue-nginx-test <application image>

You can then run the resulting image via:
docker run <application image>
EOF
